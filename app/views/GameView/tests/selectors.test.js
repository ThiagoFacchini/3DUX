// @noflow
// import Immutable from 'seamless-immutable'
// import { makeSelectGameViewDomain } from '../selectors'

// const selector = makeSelectGameViewDomain();

describe('makeSelectGameViewDomain', () => {
	it('Expect to have unit tests specified', () => {
		expect(true).toEqual(false)
	})
})
